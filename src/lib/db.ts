// Future integration point:
// - pg Pool connection to PostgreSQL
// - Flyway migrations in /db/migrations
// - Query products and leads by active site id (process.env.SITE)
// - Replace src/data/sites/<site-id>/products.ts reads with query functions here

export {};
